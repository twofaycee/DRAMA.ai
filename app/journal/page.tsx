'use client'
import{useEffect,useState}from'react';import Link from'next/link'
const prompts=['What problem would you willingly spend the next three years becoming extremely good at solving?','What are you doing today that your future self will wish you stopped doing?','If nobody could see you fail, what would you attempt?','What skill could you master that would make you difficult to replace?','Are you trying to look successful or become successful?']
export default function Journal(){const[notes,setNotes]=useState('');useEffect(()=>setNotes(localStorage.getItem('abb_notes')||''),[]);const save=(v:string)=>{setNotes(v);localStorage.setItem('abb_notes',v)};return <main><nav className="nav"><div className="shell navInner"><Link href="/" className="brand">AI BUSINESS BUILDER<span>.</span></Link><Link href="/dashboard">← Dashboard</Link></div></nav><div className="shell journal"><div className="eyebrow">THE WEALTH JOURNAL</div><h1>Think like an owner.</h1><p className="lead">Slow down, tell yourself the truth, and turn reflection into decisions.</p><div className="promptGrid">{prompts.map((p,i)=><div className="prompt" key={p}><span>0{i+1}</span><p>{p}</p></div>)}</div><textarea value={notes} onChange={e=>save(e.target.value)} placeholder="Write here. Your notes save on this device."/><small>Saved locally on this device.</small></div></main>}